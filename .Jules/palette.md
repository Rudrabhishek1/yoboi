## 2026-05-02 - [Tooltips on IconButtons]
**Learning:** Icon-only buttons (IconButton) in Flutter applications lack native screen reader context and hover text by default, which is a major accessibility issue.
**Action:** Always add the 'tooltip' property to 'IconButton' widgets to ensure accessibility parity with web ARIA labels.
