## 2024-06-09 - Add tooltips to icon-only buttons
**Learning:** In Flutter, provide ARIA-equivalent accessibility labels for icon-only buttons by explicitly setting the `tooltip` property on `IconButton` widgets.
**Action:** Always verify `IconButton` widgets have a `tooltip` property.
